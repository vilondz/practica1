#include <iostream>
#include "tape.h"
int main (){
    Tape cinta1;
   // std::cout << "Creamos el objeto" << std::endl;
    cinta1.drow_tape(std::cout);
   // std::cout << "dibujamos la cinta" << std::endl;
    cinta1.drow_tape(std::cout);
  return 0;
}
